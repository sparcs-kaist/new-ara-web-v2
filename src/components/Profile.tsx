'use client';
import Image from "next/image";
import { useTranslation } from 'react-i18next';

const Profile = () => {
    return (
        <div>
            
        </div>
    );
};

export default Profile;